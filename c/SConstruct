env = Environment()
env.Append(CPPFLAGS=['-pthread', '-Wall', '-g'])
env.Program(target = 'bin/bitree', source = ["bitree.c"])
env.Program(target = 'bin/code', source = ["code.c"])
env.Program(target = 'bin/vec', source = ["vec.c"])
env.Program(target = 'bin/udpproxy', source = ["net/udpproxy.c"])
